# DOBOT机械臂键盘控制系统使用指南

## 概述

本指南介绍了基于示例文件学习经验开发的增强版DOBOT机械臂键盘控制系统。该系统融合了关节级控制和末端执行器控制的优势，提供了直观易用的交互界面。

## 系统特性

### 1. 双模式控制
- **关节控制模式**: 直接控制各关节角度
- **末端执行器控制模式**: 通过笛卡尔坐标控制末端位置

### 2. 安全特性
- 软限位保护（±170°）
- 平滑运动控制
- 安全预设姿态
- 实时状态监控

### 3. 高级功能
- 实时位姿反馈
- TF坐标变换
- 预设姿态快速调用
- 动态参数调节

## 安装与启动

### 1. 环境准备
```bash
# 设置DOBOT型号
export DOBOT_TYPE=cr5

# 启动仿真环境
ros2 launch dobot_gazebo gazebo_moveit.launch.py
```

### 2. 启动键盘控制

#### 方式一：基础键盘控制
```bash
# 新终端中运行
ros2 run dobot_demo keyboard_control
```

#### 方式二：增强版键盘控制（推荐）
```bash
# 新终端中运行
ros2 run dobot_demo enhanced_keyboard_control
```

## 控制指令详解

### 基础键盘控制

#### 关节选择与控制
```
1-6: 选择要控制的关节
+  : 增加选中关节角度 (1°)
-  : 减少选中关节角度 (1°)
R  : 回到安全姿态
G  : 显示当前状态
H  : 显示帮助
QUIT: 退出程序
```

#### 使用示例
```bash
输入命令: 1          # 选择关节1
✓ 已选择关节 1
输入命令: +          # 增加关节1角度
关节 1 增量 +1.0° → 目标 +1.0°
```

### 增强版键盘控制

#### 模式控制
```
M: 切换控制模式 (关节/末端)
H: 显示当前状态
R: 回到初始姿态
S: 回到安全姿态
Q: 退出程序
```

#### 关节控制模式
```
1-6: 选择关节1-6
+/-: 增加/减少选中关节角度 (2°)
-/=: 选择关节6
```

#### 末端执行器控制模式
```
W/S: X轴前进/后退 (5mm)
A/D: Y轴左移/右移 (5mm)
Z/X: Z轴上升/下降 (5mm)
```

#### 预设姿态
```
F1: 初始姿态
F2: 预设姿态2
F3: 预设姿态3
F4: 预设姿态4
```

## 使用场景与示例

### 场景1：关节精确控制
```bash
# 启动增强版控制
ros2 run dobot_demo enhanced_keyboard_control

# 选择并移动关节2
输入命令: 2
输入命令: +++
输入命令: H
```

### 场景2：末端位置控制
```bash
# 切换到末端控制模式
输入命令: M

# 移动末端执行器
输入命令: W  # X轴前进
输入命令: D  # Y轴右移
输入命令: Z  # Z轴上升

# 查看当前位姿
输入命令: H
```

### 场景3：快速姿态切换
```bash
# 回到安全姿态
输入命令: S

# 调用预设姿态
输入命令: F2

# 回到初始姿态
输入命令: R
```

## 技术架构

### 控制流程
```
用户输入 → 指令解析 → 控制逻辑 → Action发送 → 控制器执行 → Gazebo仿真
```

### 核心组件
1. **EnhancedKeyboardControl节点**: 主控制逻辑
2. **ActionClient**: 与控制器通信
3. **TF转换器**: 坐标系变换
4. **逆运动学求解**: 末端位置到关节角度转换
5. **控制循环**: 实时状态监控和平滑控制

### 关键参数
- `JOINT_STEP_DEG`: 关节步长（默认2°）
- `EE_STEP_MM`: 末端步长（默认5mm）
- `MAX_DEG`: 软限位（±170°）
- `CONTROL_FREQUENCY`: 控制频率（20Hz）

## 安全机制

### 1. 关节限位
- 硬限位：URDF文件定义
- 软限位：代码中的±170°限制

### 2. 运动平滑
- P控制器实现平滑过渡
- 避免突变指令
- 实时误差监控

### 3. 异常处理
- 关节状态缺失检测
- Action通信失败处理
- 逆运动学求解失败处理

## 扩展开发

### 1. 自定义参数
```python
# 在EnhancedKeyboardControl.__init__中修改
self.JOINT_STEP_DEG = 1.0  # 减小步长提高精度
self.EE_STEP_MM = 2.0      # 减小步长提高精度
```

### 2. 添加新预设姿态
```python
def go_custom_pose(self):
    custom_pose = [0, math.radians(60), math.radians(-60), 0, 0, 0]
    self.send_joint_goal(custom_pose, 3.0)
```

### 3. 扩展键盘映射
```python
elif command == "F5":
    node.go_custom_pose()
```

## 故障排除

### 1. 常见问题

#### 问题：无法获取关节状态
```
错误：无法获取关节状态，请确保仿真环境正在运行
```
**解决方案**：
- 确保Gazebo仿真已启动
- 确认controller已正确加载
- 检查/joint_states话题是否正常发布

#### 问题：控制器未响应
```
关节目标被拒绝
```
**解决方案**：
- 检查控制器是否处于active状态
- 确认Action服务是否正常运行
- 检查目标角度是否超出限位

#### 问题：末端控制不准确
```
逆运动学求解失败
```
**解决方案**：
- 确保目标位置在机械臂工作空间内
- 检查TF变换是否正常
- 调整逆运动学参数

### 2. 调试命令

#### 检查系统状态
```bash
# 检查关节状态
ros2 topic echo /joint_states

# 检查控制器状态
ros2 control list_controllers

# 检查TF变换
ros2 run tf2_tools view_frames.py
```

#### 检查Action服务
```bash
# 检查Action服务
ros2 action list
ros2 action info /cr5_group_controller/follow_joint_trajectory
```

## 总结

本键盘控制系统综合了示例文件的最佳实践，提供了：

1. **直观的用户界面**：简单易用的键盘指令
2. **双模式控制**：关节和末端控制的灵活切换
3. **安全可靠**：完善的安全机制和异常处理
4. **高度可扩展**：易于添加新功能和自定义参数

通过学习和应用这些控制技术，您可以更好地理解和掌握ROS2机器人控制的核心概念，为更复杂的机器人应用打下坚实基础。